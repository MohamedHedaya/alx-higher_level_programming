#!/usr/bin/python3

def square_matrix_simple(matrix=[]):
    result = []
    for row in matrix:
        for column in matrix[row]:
            result[row][column] = matrix[row][column] * matrix[row][column]
    return result
